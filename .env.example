# Happy Server docker 环境配置示例
# cp .env.example .env

# 应用配置
NODE_ENV=production
PORT=3005

# 数据库配置（全量部署）
POSTGRES_DB=happy-server
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/happy-server

# Redis 配置（全量部署）
REDIS_URL=redis://redis:6379

# MinIO/S3 配置
S3_HOST=minio
S3_PORT=9000
S3_USE_SSL=false
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin123
S3_BUCKET=happy-files
S3_PUBLIC_URL=http://localhost:9000/happy-files

# MinIO 管理配置
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123

# 安全配置（必须修改为安全的随机字符串）
HANDY_MASTER_SECRET=change-this-to-a-secure-random-string
SEED=change-this-to-a-secure-random-string

# 简化部署配置（连接外部服务时使用）
# 如果使用 docker-compose.simple.yml，取消注释并修改以下配置
# DATABASE_URL_EXTERNAL=postgresql://username:password@host.docker.internal:5432/happy-server
# REDIS_URL_EXTERNAL=redis://:password@host.docker.internal:6379/4
# S3_HOST_EXTERNAL=host.docker.internal
# HAPPY_SERVER_PORT=3456